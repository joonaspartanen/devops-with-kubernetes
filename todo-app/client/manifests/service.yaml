apiVersion: v1
kind: Service
metadata:
  name: todo-app-client-svc
  namespace: todo-namespace
spec:
  type: ClusterIP
  selector:
    app: todo-app-client
  ports:
    - port: 2347
      protocol: TCP
      targetPort: 8040
